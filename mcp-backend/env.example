# Server
PORT=8787
HOST=0.0.0.0

# Repo root (monorepo). Set this when launching from subdirectories.
SCA_REPO_ROOT=..

# Logging
SCA_LOG_DIR=./logs

# Ollama (local)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3

# Finisher settings
SCA_MAX_TURNS=16

# MCP tool server (stdio)
SCA_TOOLSERVER_CMD=npx
# If SCA_TOOLSERVER_ARGS is not set, defaults to: tsx <repoRoot>/apps/cli/src/mcp/toolServer.ts
SCA_TOOLSERVER_ARGS=
SCA_TOOLSERVER_PATH=

# Permissions
SCA_ALLOW_WRITE=false
SCA_ALLOW_EXEC=false

# Optional
CORS_ORIGINS=
RATE_LIMIT_MAX=60
RATE_LIMIT_WINDOW=1 minute


