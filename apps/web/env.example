# Web UI (Vite) environment variables
#
# Copy to .env.local for local dev:
#   cp env.example .env.local
#
# IMPORTANT: VITE_* variables are bundled into the browser build. Do NOT store secrets here.

# Cloud API base URL (optional).
# - If the web UI is served by the same Railway service as the API, leave this empty (same-origin).
# - If the web UI is hosted separately, set this to the Cloud API base URL.
VITE_API_BASE_URL=

# Prod server reverse-proxy target (used by apps/web/server.mjs).
# For local prod:
#   CLOUD_API_BASE_URL=http://127.0.0.1:8787
CLOUD_API_BASE_URL=http://127.0.0.1:8787

# Cloud API base URL (prod default used by server.mjs if CLOUD_API_BASE_URL is unset)
# CLOUD_API_BASE_URL=https://sca-01-phase3-production.up.railway.app


