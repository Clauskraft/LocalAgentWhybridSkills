# SCA-01 Architecture

> Teknisk arkitekturdokumentation for SCA-01 "The Finisher"

---

## 🏗️ Overordnet Arkitektur

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER SURFACES                               │
│                                                                     │
│   ┌─────────┐    ┌──────────────┐    ┌──────────────┐              │
│   │   CLI   │    │ Electron UI  │    │  Cloud API   │              │
│   │ (sca)   │    │  (Phase 2)   │    │  (Phase 3)   │              │
│   └────┬────┘    └──────┬───────┘    └──────┬───────┘              │
│        │                │                    │                      │
│        └────────────────┼────────────────────┘                      │
│                         ▼                                           │
├─────────────────────────────────────────────────────────────────────┤
│                       AGENT RUNTIME                                 │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                    FinisherAgent                             │  │
│   │                                                              │  │
│   │    ┌─────────┐   ┌──────────┐   ┌──────────────────────┐   │  │
│   │    │  READ   │ → │   PLAN   │ → │  ACT (Tool Calls)    │   │  │
│   │    └─────────┘   └──────────┘   └──────────────────────┘   │  │
│   │         │                               │                   │  │
│   │         │                               ▼                   │  │
│   │         │              ┌────────────────────────────────┐   │  │
│   │         │              │          TEST / VERIFY         │   │  │
│   │         │              └────────────────────────────────┘   │  │
│   │         │                               │                   │  │
│   │         └───────────────────────────────┘                   │  │
│   │                         │                                   │  │
│   │                         ▼                                   │  │
│   │              ┌──────────────────────┐                       │  │
│   │              │       REPORT         │                       │  │
│   │              │ (Update Blackboard)  │                       │  │
│   │              └──────────────────────┘                       │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                       CORE SERVICES                                 │
│                                                                     │
│   ┌─────────────┐  ┌─────────────┐  ┌────────────────────────────┐ │
│   │   Policy    │  │   HyperLog  │  │    SelfHealingAdapter      │ │
│   │   Engine    │  │   (JSONL)   │  │    (Retry + Recovery)      │ │
│   └─────────────┘  └─────────────┘  └────────────────────────────┘ │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                       INTEGRATIONS                                  │
│                                                                     │
│   ┌─────────────────┐         ┌─────────────────────────────────┐  │
│   │      Ollama     │         │        MCP Tool/Agent Bus       │  │
│   │   /api/chat     │         │                                 │  │
│   │  (Tool Calling) │         │  ┌─────────┐    ┌────────────┐  │  │
│   └────────┬────────┘         │  │  stdio  │    │   HTTP     │  │  │
│            │                  │  │ (local) │    │ (Phase 3)  │  │  │
│            ▼                  │  └────┬────┘    └─────┬──────┘  │  │
│   ┌─────────────────┐         │       │               │         │  │
│   │  LLM Response   │         │       ▼               ▼         │  │
│   │  + Tool Calls   │         │  ┌────────────────────────────┐ │  │
│   └─────────────────┘         │  │      MCP Tool Server       │ │  │
│                               │  │                            │ │  │
│                               │  │  • File tools              │ │  │
│                               │  │  • Shell tools             │ │  │
│                               │  │  • Process tools           │ │  │
│                               │  │  • Network tools           │ │  │
│                               │  │  • Clipboard tools         │ │  │
│                               │  │  • Browser tools           │ │  │
│                               │  └────────────────────────────┘ │  │
│                               └─────────────────────────────────┘  │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                         STATE                                       │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                   Blackboard (Markdown)                      │  │
│   │                                                              │  │
│   │   docs/HANDOVER_LOG.md  ←  Primary state                     │  │
│   │   docs/BLACKBOARD.md    ←  Optional extended state           │  │
│   │   logs/*.jsonl          ←  Audit trail (HyperLog)            │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Agent Loop Sekvens

```
sequenceDiagram
    participant U as User
    participant A as Agent
    participant O as Ollama
    participant M as MCP Server
    participant B as Blackboard

    U->>A: sca run --goal "..."
    A->>M: tools/list
    M-->>A: Available tools
    
    A->>O: chat(system + goal, tools)
    O-->>A: tool_calls: [read_handover_log]
    
    A->>M: tools/call(read_handover_log)
    M->>B: Read docs/HANDOVER_LOG.md
    B-->>M: Content
    M-->>A: Tool result
    
    A->>O: chat(messages + tool result)
    O-->>A: tool_calls: [read_file, write_file, ...]
    
    loop Until no more tool calls
        A->>M: tools/call(...)
        M-->>A: Result
        A->>O: chat(messages + results)
        O-->>A: More tool calls or final response
    end
    
    A-->>U: Executive summary
```

---

## 🔐 Security Architecture

### Policy Engine Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                      POLICY EVALUATION                              │
│                                                                     │
│   Request ──────────────────────────────────────────────────────►   │
│       │                                                             │
│       ▼                                                             │
│   ┌───────────────┐                                                 │
│   │ Is path/cmd   │──── YES ────► BLOCKED (Critical)                │
│   │ in blocklist? │                                                 │
│   └───────┬───────┘                                                 │
│           │ NO                                                      │
│           ▼                                                         │
│   ┌───────────────┐                                                 │
│   │ Is path in    │──── YES ────► ALLOWED (Low risk)                │
│   │ safe dirs?    │                                                 │
│   └───────┬───────┘                                                 │
│           │ NO                                                      │
│           ▼                                                         │
│   ┌───────────────┐                                                 │
│   │ Is full       │──── NO ─────► BLOCKED (Needs flag)              │
│   │ access on?    │                                                 │
│   └───────┬───────┘                                                 │
│           │ YES                                                     │
│           ▼                                                         │
│   ┌───────────────┐                                                 │
│   │ Is operation  │──── NO ─────► ALLOWED (Medium risk)             │
│   │ high risk?    │                                                 │
│   └───────┬───────┘                                                 │
│           │ YES                                                     │
│           ▼                                                         │
│   ┌───────────────┐                                                 │
│   │ Is auto       │──── YES ────► ALLOWED (High risk, logged)       │
│   │ approve on?   │                                                 │
│   └───────┬───────┘                                                 │
│           │ NO                                                      │
│           ▼                                                         │
│   ┌───────────────────────────────────────────┐                     │
│   │        APPROVAL QUEUE                     │                     │
│   │                                           │                     │
│   │   Wait for user: APPROVE / REJECT         │                     │
│   └───────────────────────────────────────────┘                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Approval Queue Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                      APPROVAL SYSTEM                                │
│                                                                     │
│   ┌─────────────┐                                                   │
│   │   Policy    │                                                   │
│   │   Engine    │                                                   │
│   └──────┬──────┘                                                   │
│          │ requiresApproval=true                                    │
│          ▼                                                          │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                  Approval Queue                              │  │
│   │                                                              │  │
│   │   pending: Map<id, ApprovalRequest>                          │  │
│   │   history: ApprovalRequest[]                                 │  │
│   │                                                              │  │
│   │   Events:                                                    │  │
│   │     • 'request' → New approval needed                        │  │
│   │     • 'resolved' → Approved/Rejected/Timeout                 │  │
│   └─────────────────────────────────────────────────────────────┘  │
│          │                              ▲                          │
│          │ emit('request')              │ approve(id) / reject(id) │
│          ▼                              │                          │
│   ┌──────────────┐              ┌──────────────┐                   │
│   │  Electron UI │              │     CLI      │                   │
│   │   (Desktop)  │              │  (Terminal)  │                   │
│   └──────────────┘              └──────────────┘                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📦 Component Details

### Phase 1 Components

| Component | File | Responsibility |
|-----------|------|----------------|
| CLI | `cli.ts` | Command parsing, user interface |
| Config | `config.ts` | Environment loading |
| FinisherAgent | `agent/FinisherAgent.ts` | Agent loop orchestration |
| OllamaChatClient | `ollama/ollamaChatClient.ts` | LLM communication |
| McpToolClient | `mcp/mcpToolClient.ts` | MCP client (stdio) |
| ToolServer | `mcp/toolServer.ts` | MCP server with 6 tools |
| HyperLog | `logging/hyperlog.ts` | Structured logging |
| PathPolicy | `security/pathPolicy.ts` | Path validation |

### Phase 2 Additional Components

| Component | File | Responsibility |
|-----------|------|----------------|
| DesktopAgent | `agent/DesktopAgent.ts` | Extended agent |
| PolicyEngine | `security/policy.ts` | Risk evaluation |
| ApprovalQueue | `approval/approvalQueue.ts` | Approval management |
| SelfHealingAdapter | `selfhealing/SelfHealingAdapter.ts` | Error recovery |
| ToolServerFull | `mcp/toolServerFull.ts` | 20+ tools |
| ElectronMain | `ui/main.ts` | Desktop UI main process |
| ShellTools | `tools/shellTools.ts` | Command execution |
| FileTools | `tools/fileTools.ts` | Full filesystem access |
| SystemTools | `tools/systemTools.ts` | Process management |
| ClipboardTools | `tools/clipboardTools.ts` | Clipboard access |
| BrowserTools | `tools/browserTools.ts` | URL/HTTP/screenshot |
| NetworkTools | `tools/networkTools.ts` | Connectivity/ports |

---

## 🔌 MCP Protocol Usage

### Tool Registration

```typescript
server.tool(
  "tool_name",           // Unique name
  "Description",         // For LLM
  { param: z.string() }, // Zod schema
  async ({ param }) => {
    // Implementation
    return { content: [{ type: "text", text: result }] };
  }
);
```

### Tool Categories

```
┌─────────────────────────────────────────────────────────────────────┐
│                         MCP TOOL SERVER                             │
│                                                                     │
│   File Tools ────────────────────────────────────────────────────   │
│   │ read_file, write_file, list_directory, file_info, search_files │
│                                                                     │
│   Shell Tools ───────────────────────────────────────────────────   │
│   │ run_shell, run_powershell                                       │
│                                                                     │
│   System Tools ──────────────────────────────────────────────────   │
│   │ system_info, network_interfaces, environment_vars               │
│   │ list_processes, kill_process                                    │
│                                                                     │
│   Clipboard Tools ───────────────────────────────────────────────   │
│   │ clipboard_read, clipboard_write                                 │
│                                                                     │
│   Browser Tools ─────────────────────────────────────────────────   │
│   │ open_url, http_request, screenshot                              │
│                                                                     │
│   Network Tools ─────────────────────────────────────────────────   │
│   │ check_port, check_connectivity, get_local_ips                   │
│                                                                     │
│   Meta Tools ────────────────────────────────────────────────────   │
│   │ toolserver_info, approval_status                                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Observability

### HyperLog Events

```jsonl
{"ts":"2025-01-01T12:00:00.000Z","level":"info","event":"agent.start","message":"Starting agent","context":{"model":"qwen3"}}
{"ts":"2025-01-01T12:00:01.000Z","level":"info","event":"agent.tool_calls","message":"Tool calls","context":{"tools":["read_file"]}}
{"ts":"2025-01-01T12:00:02.000Z","level":"security","event":"file.write.policy","message":"Write blocked","context":{"path":"/etc/hosts"}}
```

### Log Levels

| Level | Purpose |
|-------|---------|
| `debug` | Verbose development info |
| `info` | Normal operations |
| `warn` | Non-critical issues |
| `error` | Failures |
| `security` | Security-relevant events (separate file) |

---

## 🚀 Deployment

### Local Development
```bash
npm run dev -- run
```

### Production Build
```bash
npm run build
npm run sca -- run
```

### Electron Desktop
```bash
npm run build:ui  # Creates dist/*.exe
```

---

## 🔮 Future: Phase 3 & 4

### Phase 3: Cloud Mode
- MCP over Streamable HTTP
- mTLS authentication
- Secret management (KMS)
- Immutable audit logs

### Phase 4: Agent Mesh
- Agent registry
- Multi-agent coordination
- Parallel tool calling
- Discovery protocol

---

*Architecture documentation for SCA-01 "The Finisher"*

