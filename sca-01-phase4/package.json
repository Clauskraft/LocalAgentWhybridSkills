{
  "name": "sca-01-phase4",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "SCA-01 Phase 4: Agent-Mesh - Multi-agent coordination",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node build/cli.js",
    "registry": "tsx src/registry/agentRegistry.ts",
    "orchestrator": "tsx src/orchestrator/meshOrchestrator.ts",
    "test": "vitest run",
    "test:smoke": "vitest run -t \"\\(smoke\\)\"",
    "test:node": "npm run build && node --test build/**/*.node.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.8.0",
    "vitest": "^2.0.0"
  }
}

